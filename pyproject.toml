[project]
name = "gemini-fastapi"
version = "1.0.0"
description = "FastAPI Server built on Gemini Web API"
readme = "README.md"
requires-python = "==3.13.*"
dependencies = [
    "curl-cffi>=0.15.0",
    "fastapi>=0.136.1",
    "gemini-webapi>=2.0.0",
    "httptools>=0.7.1",
    "lmdb>=2.2.0",
    "loguru>=0.7.3",
    "orjson>=3.11.9",
    "pydantic-settings[yaml]>=2.14.1",
    "uvicorn>=0.47.0",
    "uvloop>=0.22.1; sys_platform != 'win32'",
]

[project.urls]
Repository = "https://github.com/Nativu5/Gemini-FastAPI"

[project.optional-dependencies]
dev = [
    "pyright",
    "ruff",
    "ty",
]

[dependency-groups]
dev = [
    "gemini-fastapi[dev]",
]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "F",    # pyflakes
    "W",    # pycodestyle warnings
    "I",    # isort
    "UP",   # pyupgrade
    "B",    # flake8-bugbear
    "C4",   # flake8-comprehensions
    "SIM",  # flake8-simplify
    "RUF",  # ruff-specific rules
    "TID",  # flake8-tidy-imports
]
ignore = [
    "E501", # line too long
]

[tool.ruff.lint.flake8-bugbear]
extend-immutable-calls = [
    "fastapi.Depends",
    "fastapi.Query",
    "fastapi.security.HTTPBearer",
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.uv.sources]
gemini-webapi = { git = "https://github.com/luuquangvu/Gemini-API.git", rev = "enable-guest-mode" }
